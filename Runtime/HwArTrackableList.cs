namespace UnityEngine.XR.AREngine
{
    public class HwArTrackableList
    {

    }
}

/*
void

HwArTrackableList_acquireItem(const HwArSession *session, const HwArTrackableList *trackableList, int32_t index, HwArTrackable **outTrackable)

从可跟踪列表中获取指定index的对象。

void

HwArTrackableList_create(const HwArSession *session, HwArTrackableList **outTrackableList)

创建一个可跟踪的列表对象。

void

HwArTrackableList_destroy(HwArTrackableList *trackableList)

释放可跟踪列表对象使用的内存，以及它持有的所有锚点引用。

void

HwArTrackableList_getSize(const HwArSession *session, const HwArTrackableList *trackableList, int32_t *outSize)

检索此列表中的可跟踪对象的数量。
*/