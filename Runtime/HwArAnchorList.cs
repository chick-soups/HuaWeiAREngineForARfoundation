namespace UnityEngine.XR.AREngine
{
    public class HwArAnchorList
    {

    }
}

/*void

HwArAnchorList_acquireItem(const HwArSession *session, const HwArAnchorList *anchorList, int32_t index, HwArAnchor **outAnchor)

从锚点列表中获取index位置的锚点信息。

void

HwArAnchorList_addItem(const HwArSession *session, HwArAnchorList *anchorList, HwArAnchor *anchor)

将锚点加入到锚点列表中。

void

HwArAnchorList_create(const HwArSession *session, HwArAnchorList **outAnchorList)

创建一个锚点列表。

void

HwArAnchorList_destroy(HwArAnchorList *anchorList)

释放一个锚点列表对象的内存，同时释放它所持有列表锚点引用。
*/